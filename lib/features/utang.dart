import 'package:flutter/material.dart';

class UtangPage extends StatelessWidget {
  const UtangPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Utang Page'),
      ),
    );
  }
}